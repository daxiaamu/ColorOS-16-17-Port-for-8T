# ColorOS 17 实测状态与验收边界

更新：2026-09-20。本分支记录 OnePlus 15 PLK110 → OnePlus 8T KB2000 的开发移植。供体为 PLK110_11.C.61 对应 ColorOS 17.0.0.100(SP09CN01)，Android 17/API 37；目标使用经过验证的 8T 4.19 内核与硬件底层。离线分析供体包不意味着可以操作供体手机。

先读本页，再按 SKILL.md 路由阅读。本文优先于同目录 ColorOS 16 历史记录中的“ColorOS 17 尚未验证”，但不把 OS16 的硬件、DDR、固件覆盖面自动扩展到 OS17。

## 三个不同的状态

| 层级 | 已有证据 | 不代表什么 |
|---|---|---|
| 完整开发包 dev24 | 实际 TWRP 整包安装、写入范围读回、经用户授权 Format Data 后正常首次启动 | 不是 enforcing、无 ROOT 或所有硬件验收完成的正式版 |
| 当前设备增量状态 | 截至 dev47：system 为 dev46，system_ext 为 dev46，my_product 为 dev47；保留分享/性能/山之道/IMS，后继范围见下表；完整回读和重启通过 | 旧 dev24 ZIP 不会自动获得后继修复 |
| dev35整合 | AOD默认0、MOD菜单/图标、版本作者日期、旧山之道索引去重；默认动画等级保留3 | 已保存的用户设置不强制覆盖；不等于新全量ZIP已生成 |

dev24 完整 ZIP SHA256：`5447eb8fdf12f0315163fd03af76354f01621bd99b51e11740fb81f9979c3565`。这只是历史开发产物的身份锚点，本仓库不提供该 ROM。清装首启约198秒，不能用首次 dexopt 阶段衡量日常性能。开发 boot 为 permissive，含既有 ReSukiSU 内核基线；不继承 OS16 的“无 ROOT 发布”结论。

## 已完成与仍需验证

- 已开机；启动故障的处理见 [启动与框架](os17-boot-framework.md)。日志中的 boot_completed、稳定进程、实体屏可操作是不同证据。
- MOD 入口、关于顶部卡片、主题栏目与全景息屏显示设置已恢复；卡片遮罩修复实机可见。
- 预装安装源回到 Super；44个迁移文件的哈希和只读挂载已验证。已有 /data/app 实例仍属于用户数据；清装后的全部44文件复核未作为本轮完成项。
- 无线充电共同能力声明已纠正，设置与磁贴入口均验收；有线反向充电保留。
- 互传控制中心入口添加/移除/重加通过；后继普通分享入口修复获得用户“可用”确认，不等于 agent 观察到真实文件传输。
- 分身上限20，原生边界探针与设置文案通过；没有创建并启动全部20个分身。
- AOD默认效果实体显示通过；增强无缝模式有配套内核依赖，不能只以DOZE状态或截图验收。
- dev32语音服务连续约12分钟未再出现原先Java崩溃、PID稳定；未完成实体语音唤醒功能测试。Perfetto重启后正常并成功追踪。
- 锁屏到桌面时钟曾有原生交接和用户成功反馈；曾观察到等级相关原生关闭条件；用户确认默认等级3没有问题，保留3，不据此强制修改默认或用户选择。
- 无线子系统回调空指针已由 dev42 协议对齐修复并完成短期观察；SIM 间歇掉卡仍未定位。部分解锁长帧、完整 enforcing 和广泛硬件回归仍未闭环。

## dev35已合入范围

AOD默认“默认效果”(0)、动画选项标题改“动画效果等级”、高级重启改普通菜单、作者链接6个图标、版本含daxiaamu与构建日期、删除补入的旧山之道索引。已同时保留分享、性能和山之道持久修复；三个分区内容、inode及标签审计均零差异，实机完整读回及重启通过。旧ZIP仍为dev24。

## 山之道黑壁纸修复

已定位新版主题缺失初始尺寸布局，补做布局后用户确认桌面和锁屏均恢复。原生引擎已验证修复容器可解包并更新缓存，dev34持久分区刷入及冷启动验证通过，用户确认三款切换均正常；临时Shell ROOT已撤销、管理器已卸载。见[诊断、最小修复与收尾](os17-mountain-wallpaper.md)。这与旧索引重复、OneShot等级门槛分别处理。

## dev36电话修复

SIM和网络原已识别，但OS17 IMS删除HIDL实现，8T无法注册IMS。恢复限定的HIDL适配层后，MMTEL注册及语音/短信能力正常，用户实测电话回复“正常了”。未更换基带或清除用户数据；短信收发尚未实测。见[IMS诊断与修复](os17-ims-hidl.md)。完整ZIP仍为dev24。

## 2026-09-20 后继修复与待验收

本表覆盖 dev37–47，状态优先于旧候选文档。设备版本显示已核对为 `KB2000_17.0.0.100_PORT_DEV01_daxiaamu_20260920`，display.id 与 display.id.show 一致；供体 OTA 身份未改。

| 范围 | 当前证据 | 尚未完成/不可外推 |
|---|---|---|
| dev37 安装器 | 原安装器已恢复；dev37e 仅离线候选 | 新原生/COUI 安装器未在当前系统启用；不能报已完成 |
| dev38 晕动舒缓 | 原生传感器兼容、六项生命周期、用户确认圆点跟随运动 | 实际乘车自动进入/退出 |
| dev39 音量 | 3x 原生能力开启，2x/4x 未开，音量设置保留 | 最大音量、声压/声学增益 |
| dev41 MOD 智慧感知 | 总入口仅列实际存在的隔空手势，跳转/返回通过 | 智慧弹码不等于智感支付；缺失项目未伪造 |
| dev42 无线回调 | 双实例协议回调、约310秒 PID 稳定，原崩溃未复现 | SIM 间歇掉卡根因与长期稳定性 |
| dev44 隔空接听 | 原生 Camera2/CPU 模型链通过；用户确认真实来电成功 | 其它隔空手势场景 |
| dev45 相机与相册 | 相机三项既有补丁已实测；相册原包恢复注册 | 相册实况照片慢动作编辑 |
| dev46 闪充展示 | ART 用例、文件/镜像、重启、普通 USB 反例和颜色能力通过 | 65W 动画/颜色与充满实体过渡 |
| dev47 游戏助手 | 原包恢复注册、后台进程出现、首轮无应用退出记录 | 待用户解锁、指定游戏，验证侧边栏与工具 |

相关机制从 SKILL.md 的专项路由进入。SIM 曾在旧电话正常基线也延迟掉卡；不要将 IMS 修复、无线回调修复和一次双 LOADED 合并成“SIM 已永久修好”。

明确未合入：视频通话美颜候选；计算器彩蛋因用户选择暂不修；真我蓝牙音频分离跳过；相册视频转实况导出时长 APK 补丁未应用；抬起唤醒由用户开启后停止处理。保留默认动画等级3。

### 当前分区身份锚点

这些哈希用于区分已安装组合，不是提供 ROM 下载，也不是供体通用目标值。

| 分区 | 已安装候选 | SHA256 |
|---|---|---|
| system | charge-display-dev46 | a80d4c5147ce19b3496ee8801e89f721970a59e4a6df3255fb2daa656484a45e |
| system_ext | charge-display-dev46 | 2f3bd69690b9bf74b71a9026b906d2e353520a0e84b4a51a2892d5ca9deda8cd |
| my_product | games-dev47 | 9a16a061fe93d626f27c11b0a71929dffdf022a625bdf26d95cf2303450c1c28 |

完整 ZIP 仍为 dev24。本文未宣称 dev47 已做清装或生成新的完整包。
